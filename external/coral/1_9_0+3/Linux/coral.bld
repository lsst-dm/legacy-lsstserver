# LSST build script (lssteups) for coral
#
fetch external/coral/$version/Linux/coral-${version}-linux.tar.gz
unpack_tar_and_enter coral-${version}-linux.tar.gz

mkdir -p $installdir
mv ${version}-linux/* $installdir

