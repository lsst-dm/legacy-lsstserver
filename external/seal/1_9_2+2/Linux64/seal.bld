# LSST build script (lssteups) for seal
#
fetch external/seal/$version/Linux64/seal-${version}-Linux64.tar.gz
unpack_tar_and_enter seal-${version}-Linux64.tar.gz

pkgdir=`dirname $installdir`
mkdir -p $pkgdir
mv $version $pkgdir

