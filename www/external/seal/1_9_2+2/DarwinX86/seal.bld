# LSST build script (lssteups) for seal
#
fetch external/seal/$version/DarwinX86/seal-${version}-DarwinX86.tar.gz
unpack_tar_and_enter seal-${version}-DarwinX86.tar.gz

pkgdir=`dirname $installdir`
mkdir -p $pkgdir
bash fixmeup.sh $pkgdir $version
mv $version $pkgdir

